# sigeva-front

Contrato React de la tanda **elección global** (una elección por centro, jornada en el candidato).

```bash
cd sigeva-front
npm i
npm run dev
```

Back en `http://localhost:3333`. Vite (5173) proxea `/api`.

El agente de frontend empieza por [AGENTS.md](./AGENTS.md): ahí están las tareas de **Paula** y **Sofia**.
